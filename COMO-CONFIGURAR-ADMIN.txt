===================================================================
  TRADE FOX 🦊  —  GUÍA PARA CONFIGURAR EL PANEL ADMIN
===================================================================

Esta guía explica cómo configurar el panel de administración
para que puedas agregar, editar y eliminar productos de tu
tienda directamente desde el navegador, SIN tocar código.


================================================================
  ⚠️  PASO 0 (OBLIGATORIO): SEGURIDAD DEL PANEL
================================================================

El panel de administración está PROTEGIDO con contraseña.
Nadie puede entrar a /admin sin ella: ni siquiera verá la
página, solo verá una pantalla de acceso.

La contraseña NO está en el código (para que nadie pueda verla
aunque el repositorio sea público). Se guarda como una
"variable de entorno" en Netlify.


CÓMO PONER LA CONTRASEÑA (solo la primera vez):

1. Entra en Netlify y abre tu sitio (tradefoxshop).
2. Ve a:  Site settings  →  Environment variables
3. Haz clic en "Add a variable" → "Add a single variable".
4. Rellena:
     Key:    ADMIN_PASSWORD
     Value:  (la contraseña que tú quieras)
             Ejemplo:  Fox-2024!Segura
5. Haz clic en "Deploy site" o en "Save".
6. IMPORTANTE: tienes que VOLVER A DESPLEGAR (redeploy)
   para que la contraseña surta efecto.
   Ve a "Deploys" → "Trigger deploy" → "Deploy site".


COMPROBAR QUE FUNCIONA:

1. Entra en:  tradefoxshop.netlify.app/admin
2. Debe aparecer una pantalla de acceso pidiendo la contraseña.
3. Escribe la contraseña y pulsa "Entrar".
4. Ya estás dentro. La sesión dura 8 horas; después vuelve
   a pedirla.


SI VES EL MENSAJE "Falta configurar la contraseña":
   → Significa que aún no has hecho el PASO 0.
   → Sigue los pasos anteriores.


CAMBIAR LA CONTRASEÑA EN EL FUTURO:

1. Ve a Netlify → Site settings → Environment variables.
2. Edita el valor de ADMIN_PASSWORD y pon la nueva contraseña.
3. Vuelve a desplegar (Deploys → Trigger deploy).

NOTA: Si quieres que la contraseña deje de pedirse en un
navegador antes de las 8 horas, borra las cookies del sitio.


================================================================
  LO QUE NECESITAS ANTES DE EMPEZAR
================================================================

1. Una hoja de cálculo de Google Sheets con tus productos
   (si ya la creaste para la web pública, ¡usarás la misma!)
2. Google Apps Script (se configura desde Google Sheets)


================================================================
  PASO 1: CREAR O USAR TU HOJA DE GOOGLE SHEETS
================================================================

Si ya tienes una hoja de Google Sheets con tus productos
(configurada para la web), PUEDES SALTAR ESTE PASO.

Si no:
1. Ve a:  https://sheets.new
2. Cambia el nombre de la pestaña inferior a:  Productos
3. En la FILA 1, pon estos encabezados:
     A1 → Titulo
     B1 → Categoria
     C1 → Estado
     D1 → Precio
     E1 → Imagen
4. Agrega tus productos a partir de la fila 2.
5. Haz la hoja pública:
   Archivo → Compartir → Publicar en la web → Publicar
6. Copia el ID de la hoja (lo que está entre /d/ y /edit
   en la URL del navegador).


================================================================
  PASO 2: CREAR EL APPS SCRIPT (PARA GUARDAR CAMBIOS)
================================================================

Este paso permite que el panel admin pueda GUARDAR los
productos de vuelta en tu hoja de Google Sheets.

1. Abre tu hoja de Google Sheets.
2. En el menú superior, ve a:
   Extensiones → Apps Script
   (se abre una nueva pestaña con el editor de código).

3. BORRA todo el código que aparezca y PEGA esto:

   ----------------------------------------------------------------
   function doPost(e) {
     try {
       var body = JSON.parse(e.postData.contents);
       if (body.action !== 'save') {
         return jsonResponse({ error: 'Acción no válida' }, 400);
       }
       var data = body.data;
       var ss = SpreadsheetApp.getActiveSpreadsheet();
       var sheet = ss.getSheetByName('Productos');
       if (!sheet) return jsonResponse({ error: 'Hoja no encontrada' }, 404);

       // Limpiar datos existentes (mantener encabezados)
       var lastRow = sheet.getLastRow();
       if (lastRow > 1) {
         sheet.getRange(2, 1, lastRow - 1, 5).clearContent();
       }

       // Escribir nuevos datos
       if (data && data.length > 0) {
         sheet.getRange(2, 1, data.length, 5).setValues(data);
       }

       return jsonResponse({ success: true, rows: data.length });
     } catch (err) {
       return jsonResponse({ error: err.toString() }, 500);
     }
   }

   function jsonResponse(data, code) {
     return ContentService
       .createTextOutput(JSON.stringify(data))
       .setMimeType(ContentService.MimeType.JSON);
   }
   ----------------------------------------------------------------

4. Guarda el proyecto (Ctrl + S o el icono de disquete).
5. Ponle un nombre, por ejemplo: "Trade Fox Admin API".

6. IMPORTANTE — Implementar como Web App:
   - Haz clic en "Implementar" → "Nueva implementación".
   - Tipo: "Aplicación web".
   - Ejecutar como: "Yo".
   - Quién tiene acceso: "Cualquier persona".
   - Haz clic en "Implementar".
   - Se te pedirá autorización. Haz clic en "Revisar permisos"
     → "Avanzado" → "Ir a (nombre del proyecto)" → "Permitir".
   - Se mostrará un ENLACE URL. ¡Cópialo!

7. Ese enlace es lo que necesitas para el campo
   "Google Apps Script URL" en la configuración del panel.


================================================================
  PASO 3: CONFIGURAR EL PANEL ADMIN
================================================================

1. Abre tu web y ve a la URL:
   tusitio.netlify.app/admin
   (o agregando /admin.html al final de tu enlace).

2. Haz clic en el botón "Configuración" (icono de engranaje)
   arriba a la derecha.

3. Completa los 2 campos:
   - Google Sheets ID:  pega el ID de tu hoja (Paso 1)
   - Google Apps Script URL:  pega el enlace del script (Paso 2)

4. Haz clic en "Guardar".

5. ¡Listo! El panel ya está conectado y listo para usar.


================================================================
  ¿CÓMO SUBIR IMÁGENES DE LOS PRODUCTOS?
================================================================

Como las imágenes se gestionan por enlace (URL), necesitas
subir primero la foto a internet y luego pegar el enlace
en el campo "URL de la imagen".

OPCIÓN RECOMENDADA — PostImages.org (gratis y sin cuenta):
1. Entra a:  https://postimages.org
2. Haz clic en "Elegir imágenes" y selecciona tu foto.
3. (Opcional) Marca "Sin caducidad" para que no se borre.
4. Haz clic en "Subir".
5. Cuando termine, busca el enlace que dice "Hotlink para
   foros" o "Imagen directa". Copia esa URL (debe terminar
   en .jpg, .png o similar).
6. Pega esa URL en el campo de imagen del producto.

OPCIÓN 2 — Google Drive:
1. Sube la imagen a tu Google Drive.
2. Clic derecho en la imagen → "Compartir" →
   "Cualquiera con el enlace".
3. Copia el enlace y pégalo en el campo de imagen.


================================================================
  CÓMO USAR EL PANEL ADMIN
================================================================

AGREGAR UN PRODUCTO:
1. Haz clic en "Agregar Producto".
2. Completa: Título, Categoría, Estado y Precio.
3. Pega el enlace (URL) de la imagen (ver sección anterior).
   Verás una vista previa de la imagen al pegar el enlace.
   Si no pones nada, se usará una imagen por defecto.
4. Haz clic en "Guardar Producto".

EDITAR UN PRODUCTO:
1. Busca el producto en la tabla.
2. Haz clic en el icono de lápiz ✏️.
3. Cambia lo que necesites.
4. Haz clic en "Actualizar Producto".

ELIMINAR UN PRODUCTO:
1. Busca el producto en la tabla.
2. Haz clic en el icono de papelera 🗑️.
3. Confirma la eliminación.

NOTA: Todos los cambios se guardan AUTOMÁTICAMENTE
en tu hoja de Google Sheets y se reflejan en la web.


================================================================
  SOLUCIÓN DE PROBLEMAS
================================================================

❌ "No hay hoja de Google Sheets configurada"
   → Ve a Configuración y pega tu Sheet ID.

❌ "Falta el URL de Apps Script"
   → Completa el PASO 2 de esta guía.

❌ "Los cambios no se guardan"
   → Verifica que el URL de Apps Script sea correcto.
   → Verifica que el Apps Script esté "Implementado" como web app.

❌ "La imagen no aparece / no carga"
   → Verifica que la URL termine en .jpg, .png, etc.
   → Verifica que la imagen sea pública (cualquiera pueda verla).
   → Para Google Drive: debe estar como "Cualquiera con el enlace".

❌ "Error al cargar productos"
   → Verifica que la hoja esté PUBLICADA en la web.
   → Verifica que la pestaña se llame exactamente "Productos".
   → Verifica que el Sheet ID sea correcto.


================================================================
  DATOS IMPORTANTES
================================================================

- La configuración se guarda en TU NAVEGADOR (localStorage).
  Si cambias de navegador o limpias el cache, tendrás que
  volver a configurar los campos.

- El panel está protegido por contraseña a nivel de servidor
  (Netlify Edge Function + variable de entorno ADMIN_PASSWORD).
  Ver el PASO 0 al principio de esta guía para configurarla.

- La hoja de Google Sheets debe estar PUBLICADA para que
  la web pública pueda leer los productos.

================================================================
  ¿Dudas? Revisa esta guía paso a paso con calma. 🦊🎮
================================================================
